{
  "name": "@hypershell/desktop",
  "private": true,
  "version": "0.1.9",
  "description": "HyperShell Electron desktop shell",
  "author": "HyperShell Team <team@hypershell.app>",
  "homepage": "https://github.com/tomertec/HyperShell",
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "pnpm run build && electron .",
    "dev:native": "pnpm run rebuild:native && pnpm run build && electron .",
    "build": "tsc -p tsconfig.json --noEmit && node esbuild.config.mjs",
    "build:bundle": "pnpm --filter @hypershell/ui run build && pnpm run build && pnpm run sync:renderer",
    "sync:renderer": "node ../../.tools/desktop/sync-renderer-dist.mjs",
    "rebuild:native": "electron-builder install-app-deps",
    "prepare:package": "pnpm run build:bundle",
    "icons:generate": "electron ./scripts/generate-app-icons.cjs",
    "package:win:dir": "pnpm run prepare:package && electron-builder --config electron-builder.yml --win nsis --dir --publish never",
    "package:win:unsigned": "pnpm run prepare:package && electron-builder --config electron-builder.yml --win nsis --x64 --publish never",
    "package:win:signed": "node ../../.tools/desktop/verify-signing-env.mjs && pnpm run package:win:unsigned",
    "package:mac:unsigned": "pnpm run prepare:package && electron-builder --config electron-builder.yml --mac --x64 --publish never",
    "package:linux:unsigned": "pnpm run prepare:package && electron-builder --config electron-builder.yml --linux --x64 --publish never",
    "release:manifest": "node ../../.tools/release/write-release-manifest.mjs --artifacts-dir release",
    "start": "electron .",
    "start:native": "pnpm run rebuild:native && electron .",
    "test": "vitest"
  },
  "dependencies": {
    "@hypershell/db": "workspace:^",
    "@hypershell/session-core": "workspace:*",
    "@hypershell/shared": "workspace:*",
    "better-sqlite3": "^12.9.0",
    "node-pty": "^1.0.0",
    "serialport": "^12.0.0",
    "ssh2": "^1.17.0",
    "yazl": "^3.3.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/yazl": "^3.3.1",
    "electron": "^39.8.9",
    "electron-builder": "^26.8.1",
    "esbuild": "^0.25.12"
  }
}
